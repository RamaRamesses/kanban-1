# kanban

** SERVER **

Navigate to /server and run 
`node app.js`

** CLIENT **

Client Kanban built with Vue.js.

Install the packages using npm/yarn
`npm install`

start the client
`npm run serve`
